'use client';
import { Sidebar } from '@/components/Sidebar';
import { Building2, MapPin, UsersRound } from 'lucide-react';
export default function Sedes(){const sedes=[{name:'Centro Médico Caracas',address:'Caracas · Sede principal',rooms:'12 consultorios',doctors:4,status:'Activa'},{name:'Sede Este',address:'Caracas · Próxima incorporación',rooms:'6 consultorios',doctors:0,status:'Preparación'}];return <div className="dashboard"><Sidebar role="recepcion"/><main className="main"><div className="topbar"><div><div className="muted" style={{fontSize:13}}>Administración de clínica</div><h1>Sedes</h1></div><button className="btn btn-primary"><Building2 size={16}/> Agregar sede</button></div><div className="grid-3">{sedes.map(s=><div className="card" key={s.name}><div className="row space"><div className="iconbox"><Building2/></div><span className="pill">{s.status}</span></div><h3>{s.name}</h3><p><MapPin size={14} style={{verticalAlign:'middle'}}/> {s.address}</p><div className="row" style={{marginTop:16,gap:18}}><span><strong>{s.rooms}</strong></span><span className="row"><UsersRound size={15}/><strong>{s.doctors}</strong> médicos</span></div></div>)}</div><section className="panel" style={{marginTop:18}}><h2>Cómo funciona</h2><p className="muted">Cada médico puede asociarse a una o varias sedes. La sede define consultorio, ubicación y agenda visible para recepción. En producción, estas sedes se guardarán en Neon.</p></section></main></div>}
